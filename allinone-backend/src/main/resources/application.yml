server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: workflow-backend

  datasource:
    url: ${SPRING_DATASOURCE_URL}
    driver-class-name: org.postgresql.Driver
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SPRING_MAIL_USERNAME}
    password: ${SPRING_MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  jackson:
    deserialization:
      fail-on-unknown-properties: false

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            redirect-uri: ${GOOGLE_REDIRECT_URI}
            scope:
              - email
              - profile

management:
  endpoints:
    web:
      exposure:
        include: health,info

  endpoint:
    health:
      show-details: always

app:
  frontend-url: ${APP_FRONTEND_URL:https://allinone-rolpa.vercel.app}

  upload:
    dir: uploads
    cleanup-days: 30
    cleanup-cron: "0 0 3 * * ?"

  cors:
    origins: ${CORS_ALLOWED_ORIGINS:https://allinone-rolpa.vercel.app}

  oauth2:
    redirect-uri: ${APP_OAUTH2_REDIRECT_URI:https://allinone-rolpa.vercel.app/oauth2/callback}

  jwt:
    secret: ${APP_JWT_SECRET}
    expiration-ms: 2592000000

  auth:
    allowed-emails: ${ALLOWED_EMAILS}
    default-password: ${APP_DEFAULT_PASSWORD}
